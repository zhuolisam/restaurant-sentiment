# resume-ranker

## How to Use?

Install all the dependencies with:

```bash
./install.sh
```

Run the Streamlilt with:

```bash
streamlit run streamlit_app.py
```

Or run it from with:

```bash
python demo.py
```
  